#!/usr/bin/env phasor

include "std/sys";
include "std/io";

// @todo Something more probably

var x: int[2][2] = [[15, 97], [61, 77]];
var y: int[2][2] = [[24, 47], [45, 81]];

fn main(args: string[]) -> int
{
    puts("Hello World!");
    putf("%M + %M = %M", x, y, x + y);
    return 0;
}

include "std/main";
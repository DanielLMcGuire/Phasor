ffiload("__phs_init");

include "./main/canary.phs";
include "./main/common.phs";

fn start() -> void {
    var cookiePtr: i64 = phsrt_init_canary();
    phsrt__common_init();
    phsrt__chk_canary(cookiePtr);
}

start();
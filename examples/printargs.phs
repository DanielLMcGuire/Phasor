#!/usr/bin/env phasor
include "stdio.phs";
include "stdsys.phs";

fn main(args: string[]) -> int {
    for (var i: int = 1; i < args.len(); i++) {
        puts(args[i]);
    }
    // puts(args); // ["script", "arg1", "arg2", etc];
    return 0;
}

shutdown(main(sys_args()));
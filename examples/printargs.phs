#!/usr/bin/env phasor
using("stdio", "stdsys");
fn main(args: array) -> int {
    for (var i = 1; i < len(args); i++) {
        puts(args[i]);
    }
    // puts(args); ["script", "arg1", "arg2", etc] 
    return 0;
}

shutdown(main(sys_args()));
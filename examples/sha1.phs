include "std/data";
include "std/sys";

fn main(args: string[]) -> int {
    if (args.len() == 2) {
        print sha1(args[1]);
        return 0;
    } else if (!isatty()) {
        print sha1(get_input());
        return 0;
    }
    return 1;
}

include "std/main";